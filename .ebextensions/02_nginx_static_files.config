# Simplified nginx configuration - let EB handle the defaults
# This config focuses on essential optimizations only

commands:
  01_nginx_optimization:
    command: |
      # Basic nginx optimizations for t4g.micro
      echo "# t4g.micro optimizations applied via EB" > /tmp/nginx_optimized
    ignoreErrors: true
